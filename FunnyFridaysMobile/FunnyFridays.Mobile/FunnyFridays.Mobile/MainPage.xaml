<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FunnyFridays.Mobile"
             xmlns:viewModel="clr-namespace:FunnyFridays.Mobile.ViewModel"
             x:Class="FunnyFridays.Mobile.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout >
                <Label Text="{Binding GeneralLocation.Latitude}"/>
                <Label Text="{Binding GeneralLocation.Longitude}"/>
            </StackLayout>
            <Label Text="Welcome to Xamarin.Forms!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
            <StackLayout >
                <Label Text="{Binding Position.Latitude}"/>
                <Label Text="{Binding Position.Longitude}"/>
            </StackLayout>
                         
            <Button Text="Get goords" Command="{Binding GetCoordsCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>